# Web Workers

- `audio.worker.ts` — pré-processamento (Tarefa 6): mono, 22050 Hz, normalizar, cortar silêncio.
- `transcribe.worker.ts` — inferência Basic Pitch (Tarefa 7). Único ficheiro autorizado a importar TensorFlow.js.
