/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/react" />

/** Injetada por `define` em vite.config.ts a partir de `package.json` —
 *  Tarefa 21, Âmbito técnico ("versão da app" no ecrã de diagnóstico e na
 *  lista permitida de telemetria). */
declare const __APP_VERSION__: string
